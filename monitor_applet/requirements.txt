# BP16B BMS Monitor Dependencies

# CAN bus support (USB adapters)
python-can>=4.0.0

# Optional CAN interface drivers (install based on your adapter)
# PCAN: pip install python-can[pcan]
# Kvaser: pip install python-can[kvaser]
# IXXAT: pip install python-can[ixxat]
# Vector: pip install python-can[vector]
# Socketcan 
